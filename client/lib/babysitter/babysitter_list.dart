import 'package:flutter/material.dart';

class BabysitterListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de Babás'),
      ),
      body: Center(
        child: Text('Conteúdo da Lista de Babás'),
      ),
    );
  }
}